import Root from "../../components/auth/Layout";
import SignUp from "../../components/auth/SignUp";

const Auth = () => {
  return (
    <Root>
      <SignUp />
    </Root>
  );
};

export default Auth;
