import colors from "./colors";

const borders = {
  basic: `1px solid ${colors.gray1}`,
  input: `1.19856px solid ${colors.gray3}`,
  sign_in_button: `1px solid ${colors.orange_storke}`,
};

export default borders;
